# Core dependencies - everything the app needs to run on the CPU.
# GPU acceleration lives in requirements-cuda.txt and is installed separately by
# install.bat, because those wheels are ~1 GB and are optional.

faster-whisper>=1.0.3
ctranslate2>=4.4.0
PyYAML>=6.0.1
requests>=2.31.0

fastapi>=0.110.0
uvicorn>=0.29.0

# Subject detection for reframing 16:9 -> 9:16 / 4:5 / 1:1.
# MUST stay below 5.0: OpenCV 5 removed CascadeClassifier and stopped shipping
# the Haar cascade XMLs, which would silently downgrade reframing to a plain
# centre crop. The <5 wheels bundle the cascades, so nothing is downloaded.
opencv-python-headless>=4.9,<5
